const ok                = require("assert").ok
    , equal             = require("assert").equal
    , build           = require("../../lib/matching").build
    
    
console.log(build(["testa", "testb", 1]));
